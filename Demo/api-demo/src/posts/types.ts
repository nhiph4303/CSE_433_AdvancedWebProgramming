export type PostData = {
  id: number | string;
  title: string;
  description: string;
};

export type NewPostData = {
  title: string;
  description: string;
};

export type SavedPostData = {
  id: number | string;
};
